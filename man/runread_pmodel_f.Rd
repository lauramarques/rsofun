% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runread_sofun_f.R
\name{runread_pmodel_f}
\alias{runread_pmodel_f}
\title{Run the P-model}
\usage{
runread_pmodel_f(
  df_drivers,
  params_modl,
  makecheck = TRUE,
  parallel = FALSE,
  ncores = 2
)
}
\arguments{
\item{df_drivers}{A nested data frame with one row for each site and columns named according to the 
arguments of function \link{runread_pmodel_f_bysite}}

\item{params_modl}{A named list of model parameters}

\item{makecheck}{A logical specifying whether checks are performed to verify forcings.}

\item{parallel}{A logical specifying whether simulations are to be parallelised (sending 
data from a certain number of sites to each core). Defaults to \code{FALSE}.}

\item{ncores}{An integer specifying the number of cores used for parallel computing. 
Defaults to 2.}
}
\value{
A data frame (tibble) with one row for each site and outputs stored in the nested
column \code{data}
}
\description{
Runs the P-model and loads output in once.
}
\examples{
mod <- runread_pmodel_f( df_drivers, params_modl, makecheck = TRUE, parallel = FALSE, ncores = 2 )

}
